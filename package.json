{
  "name": "paperpalette",
  "private": true,
  "version": "0.3.0",
  "description": "PaperPalette: Fastify image generation API",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev -w backend",
    "build": "npm run build -w backend",
    "start": "npm run start -w backend",
    "test": "npm run test -w backend"
  },
  "engines": {
    "node": ">=22"
  }
}
